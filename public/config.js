// Az API (Node szerver) címe.
//  - Üresen hagyva: az oldalt és az API-t ugyanaz a szerver szolgálja ki (helyi fejlesztés, egyszerű tárhely).
//  - GitHub Pages + külön szerver esetén: pl. 'https://impix-api.example.com' (a GitHub Actions ezt a fájlt
//    automatikusan felülírja az IMPIX_API_URL repository-változóból, lásd a README-t).
window.IMPIX_CONFIG = { apiBase: '' };
